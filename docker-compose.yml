version: "3"
services:
  deploy:
    container_name: deck-of-cards-deploy
    build: deck-of-cards-deploy
    environment:
      AWS_CONFIG_FILE: "/root/.aws/config"
    volumes:
      - ~/.aws/:/root/.aws/
      - ~/.ssh/:/root/.ssh/
      - $PWD:/code
